on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

name: pkgdown

jobs:
  pkgdown:
    uses: IndrajeetPatil/workflows/.github/workflows/pkgdown.yaml@main
    permissions:
      contents: write
