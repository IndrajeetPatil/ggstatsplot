on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: pkgdown-no-suggests

jobs:
  pkgdown-no-suggests:
    uses: IndrajeetPatil/workflows/.github/workflows/pkgdown-no-suggests.yaml@main
    with:
      pkgdown-source: r-lib/pkgdown
