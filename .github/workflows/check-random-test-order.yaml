on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: check-random-test-order

jobs:
  check-random-test-order:
    uses: IndrajeetPatil/workflows/.github/workflows/check-random-test-order.yaml@main
